---

---

<div class='qr-card'>
  <picture class='qr-card__picture'>
    <img src='/image-qr-code.png' alt='Image qr code' />
  </picture>
  <div class='qr-card__text'>
    <p>Improve your front-end skills by building projects</p>
    <p>
      Scan the QR code to visit Frontend Mentor and take your coding skills to
      the next level
    </p>
  </div>
</div>

<style>
  .qr-card {
    max-width: 20rem;
    padding: 1rem;
    margin-inline: auto;
    border-radius: 1rem;
    background-color: var(--white);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
  }
  .qr-card__picture {
    border-radius: 0.5rem;
    overflow: hidden;
    display: block;
  }
  .qr-card__text {
    padding-inline: 1rem;
    padding-block: 1.25rem;
    text-align: center;
  }
  .qr-card__text > p:first-child {
    font-size: 1.3125rem;
    margin-block-end: 0.875rem;
    font-weight: 700;
    color: var(--dark-blue);
  }
  .qr-card__text > p:last-child {
    color: var(--grayish-blue);
    /* text-wrap: wrap; */
  }
</style>
